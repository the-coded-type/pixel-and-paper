{
	"name": "pixelpaper",
	"privare": true,
	"workspaces": [
		"packages/*"
	],
    "scripts": {
        "dev:web": "npm run dev --workspace=@pixelpaper/web",
        "build:web": "npm run build --workspace=@pixelpaper/web"
    }
}